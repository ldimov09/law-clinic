import { FiterUsersPipe } from './fiter-users.pipe';

describe('FiterUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
