import { FilterCasesPipe } from './filter-cases.pipe';

describe('FilterCasesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCasesPipe();
    expect(pipe).toBeTruthy();
  });
});
